---
kind: ConfigMap
apiVersion: v1
metadata:
  name: config-04
data:
  VAULT_MOUNT_POINT: "{{ .Values.vault.mount_point }}"
  VAULT_COMMON: "{{ .Values.vault.common }}"
  VAULT_PATH: "{{ .Values.vault.path_04 }}"
  TZ: "{{ .Values.TZ }}"